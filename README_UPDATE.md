# PMC true revision

This is the restrained revision of the existing clean “Simple pricing” design.

Changes made:
- Kept the same overall layout, typography, card treatment, spacing, and page structure.
- Removed “hydrocollator packs / hydrotherapy.”
- Removed the entire “New Client Special.”
- Replaced that panel with the requested original wellness copy.
- Added more coastal blue while retaining the burgundy/cream identity.
- Kept the existing booking and gift-certificate links.
- Uses the existing `logo.png` already in the GitHub repository.

Upload `index.html` and `styles.css` to the repository root, replacing those two files.
